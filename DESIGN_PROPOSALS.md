# 家族模拟器 · 玩法扩展设计方案

> 本文是四个方向并行探索的汇总成果。每个方向由独立探索线完成多轮迭代
> （候选发散 → 批判淘汰 → 深挖自批），并经代码勘察核实落点（见附录）。
> 目标三轴：**可玩性 / 传播性 / 深度**。配套的基础设计见 `DESIGN.md`。

---

## 第 0 章 · 总览与决策记录

### 0.1 四方向一页对比

| # | 方向名 | 一句话愿景 | 所在层 | 改动面 | 速赢度 | 主要依赖 |
|---|--------|-----------|--------|--------|--------|----------|
| A | 族老的笔 | seed 执掌生老病死，玩家只在紧要关头以族老身份批注 | 决策层 | sim_core + server + 存档 v2 + live.js UI | 慢（愿景期） | 需先定存档 v2（建议在 C 之后） |
| B | 一局一史 | 每局自动长出值得转发的故事：章回族史 + 结局定论卡 | 展示层 | live.js + meta.js，零模拟改动 | **最快** | 无 |
| C | 承祧与禀性 | 断绝率从裸骰子变成人事：过继、招赘、禀性 | 模拟层 | sim_core + params + 批跑校准 | 中（中期） | 无；是 A 的机制土壤 |
| D | 族史长廊 | 每次观察变成可收藏、可对比、可约战的族史 | 元层 | meta.js + live.html，localStorage | **快** | B 的挑战码作分享载体 |

### 0.2 本轮拍板的三个决策（已定案）

1. **「族老的笔」做成可选模式。** 新世界对话框增加「族老裁决」开关，默认关闭 = 纯观察体验一字不变；开启后才出现裁决卡。两种玩法共存互不打扰。
2. **「承祧包」纳入。** 过继/招赘作为参数默认关闭的新机制落地，配批跑校准与新增测试；默认关闭时旧 seed 逐字节复现的项目红线不破。
3. **纯观察沙盘为默认正统。** 决策层与模拟深度层均为 opt-in——「宿命感、观察美、seed 确定性」是本作气质的根，一切扩展以此为界。

### 0.3 全局设计红线（每个方向都受约束）

- **确定性**：任何新随机性必须走独立 RNG 子流（`sim_core/rng.py`，SHA-256 派生即插即用）；玩家决策若引入分支，决策本身必须记入存档、可重放还原。
- **复现承诺**：一切新机制以行为开关挂入 `params.py`，默认关闭时旧 seed 逐字节一致（现有 `flavor_events`、`matchmaking_per_year` 等开关已是先例）。
- **性能预算**：≤5 万人、1000 年，年度步进毫秒级；前端每年一次全量重排约 21ms/5 万人是现状水位。
- **零依赖**：后端仅 Python 标准库；前端原生 JS + canvas（MediaRecorder 等浏览器原生 API 可用）。
- **基调**：风物志/村志/文言短句；金色暗色 UI；「听天由命」永远是合法选项。

---

## 第 1 章 · 方向 A「族老的笔」（可玩性 · 决策层 · 愿景期）

### 1.1 愿景

seed 依旧执掌生老病死的每一笔，玩家只在少数紧要关头以族老身份**批注**（赈济、延医、指婚、听天由命）。每次批注记入族史、可重放、可分享。300 年一局从「看流水」变成「决策—观赏」交替的心流，宿命感不改。

### 1.2 三轮迭代留痕

**第一轮 · 发散（7 候选）**。调研锚点：Reigns 低频高权重卡+资源均衡节拍；King of Dragon Pass 议事会把冲突浓缩成一个抉择；CK3 状态驱动的涌现故事；Wildermyth 事件卡+跨局 Legacy；放置游戏「何时 prestige」式节拍决策。
A 年度事件裁决卡｜B 家长指令全套（指婚/分家/过继/督育）｜C 一局一目标「族训」｜D 节奏器「跳到下一决策点」｜E 血脉危急救援｜F 德望/香火资源｜G 断绝复盘卡。

**第二轮 · 批判**（乐趣/深度/成本/确定性/基调，5 分制）：

| 候选 | 乐趣 | 深度 | 成本 | 确定性 | 基调 | 判决 |
|---|---|---|---|---|---|---|
| A 裁决卡 | 4 | 4 | 3 | 4 | 4 | **留，主干** |
| B 全套指令 | 3 | 5 | 2 | 4 | 2 | **否**：把沙盘变经营游戏，亲手安排姻缘杀死宿命感，且需自建适婚者信息层，成本最高 |
| C 族训 | 3 | 3 | 5 | 5 | 5 | **留，骨架** |
| D 节奏器 | 4 | — | 5 | 5 | 5 | **留，必做胶水** |
| E 救援 | 5 | 3 | 4 | 4 | 5 | **留，并入 A 作危急卡** |
| F 资源 | 3 | 3 | 4 | 4 | 3 | **否**（多资源经济版）：出戏且测试面爆炸；砍成单值「香火」并入 A |
| G 复盘卡 | 4 | 2 | 5 | 5 | 5 | **留，终局层** |

**融合结论**：族老理事会循环 = C 目标骨架 + A/E 裁决卡（危急卡为峰值）+ 单一香火 + D 智能跳进 + G 复盘终局；B 仅保留「指婚」一个动作、限频混入 A。

**第三轮 · 自批**：
- 玩家会不会无视决策？——每卡必含「听天由命」且为默认高亮，体验可无损退化回纯观察。
- 会不会毁宿命感？——救援成功率 0.5-0.8，失败文案「天命难违」；干预只改概率不改规则，决策改变即分支为「另一种命」，分支内仍逐字节确定。
- 最小可行切片：见 1.6 P1。

### 1.3 核心设计详案

**1. 族训（局目标）**：开局三选一（「三百年而香火不坠」「一门八子」「开枝至第八代」），顶栏常驻进度；达成金榜、断绝判「未竟」。底层 = 纯函数判定（读 history/stats），零 RNG、零模拟侵入。节奏 = 一局 1 总目标 + 2 小里程碑。

**2. 事件裁决卡（核心）**：y56 春，「三郎病笃，延医需一炉香」——金卡模态弹出，2-4 个选项，必含「听天由命」。触发器读模拟状态（危急/时艰迁出潮/婚配空窗/里程碑），条件模板 + 人名填槽成卡面；效果走独立 `decree` 子流 Bernoulli 结算，成功改**参数覆盖层**（如该男丁外迁权重→0、当年死亡率×0.5），失败仅耗香火；全部村志体文案。

**3. 香火（单一资源）**：顶栏一格，初始 3、上限 7；吉事 +1、凶事 −1，救援耗 1-2；可从 decisions 事件流重算。玩家在「平时攒、危时花」间做元决策。

**4. 指婚（B 残余）**：适婚男丁 5 年未娶时出卡，卡面直列 2-3 名候选（年龄/姓氏/亲等预检结果），选后走 decree 子流定成否；全局限频 ≤6 次/局，避免夺走「撮合天定」之感。

**5. 跳到下一段春秋**：一键快进至下一张卡/大事件（危急、迁出潮、断绝边缘）自动停下，消灭空转年。

**6. 断绝复盘卡**：断绝卡升级为判词页：E1-E6 原因 + 两句族志评语 + 「某年曾有一援未用」+ 一键分享海报。

**数值节奏**：常态约 1 卡/10 年、危急必出；300 年一局约 25-40 次决策，每 1-2 分钟一决；**遇卡自动暂停**（模态裁决，非异步队列），决策稀疏无需排队。卡面自足内嵌人名/年龄/状态，玩家不必先翻 UI。

### 1.4 可选模式规格（本轮决策 #1 的落地形态）

- 参数：`elder_council_enabled: bool = False` 挂入 params 行为开关区。
- **关闭时**：零行为差异、零 UI 差异、存档格式完全不变。
- **开启时**：裁决卡生效。因玩家决策引入分支，存档需升级 v2：在现有「seed+参数+年份」外**追加 `decisions` 序列**（[{year, card_id, choice}]）与香火值；重放 = 同 seed 同参数跑模拟，每到存档中记录的决策年按既定 choice 回放——分支内仍逐字节确定。
- 存档 v2 向后兼容：旧档无 decisions 字段 = 纯观察档，直接可读；新档被旧版导入时忽略 v2 字段降级为观察档。
- UI：新世界对话框加一枚开关（与其他行为开关同列），标注「试验」；封面菜单不受影响。

### 1.5 代码落点（file:line 为探索时核实值，实现时以实际为准）

- 新 RNG 子流：`sim_core/rng.py:12` STREAMS 增 `"decree"`（derive_seed 派生即用）。
- 触发与结算层：`sim_core/village.py:184` step_year 内、`_check_extinction`（village.py:532）前插决策层；危急边沿锁存 `_lineage_danger`（village.py:442）现成；「只读状态+独立子流不破坏复现」的先例即 `_flavor_events`（village.py:473）；行为开关不破坏旧行为的先例 `params.py:74-79`。
- 决策入档与重放：`sim_core/engine.py:75` run() 增 decisions 入参；`Village.__init__`（village.py:38）挂 decisions/香火；`server.py:109` World.step 收决策、`server.py:164` snapshot 回传待决卡。
- 存档 v2：`viewer/meta.js` makeSaveEntry / saveToExportDoc 增 `decisions`；重放入口 `viewer/live.js` restoreFromSave 透传。
- 卡 UI：参照现成模态卡 showExtinctCard（`viewer/live.js:1036`）；遇卡 setPlay(false)；文案挂 EV_TEXT 同层（live.js:114）。
- 跳进：jumpTo 与时间轴点击（live.js:1158 / 1014）已有底子。
- 香火显示：stats-strip（`viewer/live.html` 顶栏）加一格。
- 指婚数据：`server.py` person_payload 已含 spouse/birth；候选枚举与禁婚预检（`sim_core/kinship.py` marriage_banned）后端算好下发。
- 复盘数据源：`sim_core/village.py:545` _classify_extinction。

### 1.6 分期路线

- **P1 最小切片**：危急三选一救援卡 + 香火 + decisions 入档/重放 + 复盘判词。验收：同 seed 同决策序列逐字节还原；不同决策自然分支。
- **P2**：族训 + 智能跳进 + 平时吉凶卡（10 年 1 张）。
- **P3**：指婚卡 + 存档馆总览增「抉择史」列 + 分享海报带族训结果。

### 1.7 风险与开放问题

1. **子流污染**：干预必须只经 decree 子流与「参数覆盖层」，不得偷耗 era/match/birth/death 消耗序列，否则逐字节还原被破坏——P1 第一验收项。
2. 卡池写作量：村志体模板 30-50 条起步，文案成本勿低估。
3. 香火囤积不用：上限 7 + 凶事自然流失对冲，需数值验证。
4. 开放：断绝后「继续观察村庄」阶段是否发尾局卡；「听天由命」默认高亮能否守住观察基调，待 P1 玩测定夺。

---

## 第 2 章 · 方向 B「一局一史」（传播性 · 展示层 · 速赢期）

### 2.1 愿景

把「断绝率高」从统计结论变成叙事资产——每局自动提炼这一姓的戏剧弧线，凝成一张「盖棺定论卡」与一部「章回族史」，卡上带挑战码让朋友开局对答案。

### 2.2 三轮迭代留痕

**第一轮 · 发散（7 候选）**。调研锚点：DF legends 玩家以「史官」自居转述涌现史；Wildermyth 手写钩子+程序组装产出可复述角色故事；社交平台 3:4 竖卡、大数字锚点、「社交货币」驱动转发。
① 章回体族史自动生成｜② 戏剧弧线检测 + 结局定论卡｜③ seed 周挑战（挑战码+对比卡）｜④ 存档导出附「家史摘要」｜⑤ MediaRecorder 录谱系 webm｜⑥ 断绝讣告文案库｜⑦ 年度金句弹幕。

**第二轮 · 批判**（动机/质量上限/成本/确定性契合/基调契合）：

| 候选 | 一句判决 |
|---|---|
| ① 章回族史 | 动机真（史官转述是 DF 社区真实行为）、era 事件是天然章界、纯事件流可重建 → **幸存·主菜** |
| ② 定论卡 | 截图需 3 秒可读+大数字锚点，海报代码可复用 → **幸存·主菜**，与 ① 共用检测引擎，盖棺论即卡文案 |
| ③ 周挑战 | 冷启动需双方都在线，对比动机真实但「周」机制无必要 → **弱化**：挑战码只作卡上附字段，不做系统 |
| ④ 家史摘要 | 收 JSON 者本就要读档，独立不成立 → **并入 ②**：导出 doc 加 summary 字段 |
| ⑤ webm 录制 | 单视频无对比无重玩、编排工程量最大 → **淘汰**（系统录屏可替代） |
| ⑥ 讣告库 | 太薄 → 并入 ① 的盖棺论分档文案；⑦ 金句弹幕 → 淘汰，纯填充无转发动机 |

**结论**：一个「传世系统」= 弧线检测引擎（素材）→ 章回族史（长内容）→ 定论卡（分享物）→ 挑战码/摘要（闭环附件）。

**第三轮 · 自批**：模板感？——槽位全是真专名（人名/年份/数字），模板感被具体性稀释，且放弃开篇七律（无 LLM 必假），只做对偶联。玩家截什么？——不是 400 条村志，而是断绝盖棺卡与险断复兴曲线，故曲线必须内嵌卡中。最小切片？——检测器仅 3 条规则+一张卡即成立。

### 2.3 核心设计详案

**C1. 戏剧弧线检测器**（纯函数 `detectArcs(history, events, people)`，确定性可重放）。规则示例：
- 险断复兴：存在窗口 `lineage_male ∈ [1,2]` 持续 ≥5 年后回到 ≥5；
- 孤丁撑天：`lineage_male == 1` 连续 ≥10 年；
- 绝处成婚：`lineage_male_adult == 0` 后 3 年内出现 lineage_marriage；
- 大迁徙：`wave` 且 `emigrated ≥ 15`；血脉外散：lineage_migrated 占男丁半数；
- 劫后余生：pop 五年滑窗回撤 >50%；千年不坠 / 断绝（带 reasons）。

**C2. 章回体族史**（新侧栏 tab + txt 导出）。章界 = era 事件（约 40 年一卷），每卷题从主导弧线生成；空卷写「无事，岁稳」（史笔传统）。模板量级：卷题 40、回末评 60、盖棺论 30（按弧线组合选择，非随机）≈130 条 × 2-3 槽位。示例：
- 卷题：「第三卷 · 一线香火——天启年间，阖族男丁唯余 {name} 一人，独撑门户十有三载。」
- 回末评：「是岁添二丁，血脉复振，然天意深浅，犹未可知。」
- 盖棺论（千年）：「一姓之火燃尽千年，{gen} 代男丁如薪相积，此谓生生不息。」
- 盖棺论（E1 早夭，冷幽默档）：「香火自此无人接，姓随谱牒葬秋风。」

**C3. 结局定论卡**（3:4 竖版 1080×1440 canvas PNG）。信息层级：大标题「{姓}氏{N}年」→ 盖棺论一句 → 四行定论（起于/兴于/险断于/终于）→ 血脉曲线金线图（标注险断点）→ 谥号印（千载侯/百年伯/不禄，按存续分档）→ 挑战码 → 页脚。存续 <30 年用冷幽默短打、>100 年挽歌、>500 年史诗——防 90% 断绝率下讣告审美疲劳。

**C4. 分享闭环**（无服务器）：朋友拿卡即读懂故事；封面加「输挑战码」（base32: version|seed|overrides）→ 同局重放 → 完局弹「对照卡」（三数对比；确定性使数字必然吻合，版本差异本身成话题）。存档导出 doc 附 summary 一句话，导入即显于存档馆。

### 2.4 代码落点

- history 字段源：`village.py:521-530`（_record）；事件流 `server.py:125/139/142`；extinct payload：`village.py:540-543`；危急锁存 `village.py:442-451`。
- EV_TEXT/17 种事件：`live.js:114-143`；村志入志 `live.js:50-53、161-164`；renderChronicle `live.js:596-629`（旁加「族史」tab，setSideTab 587-594）；exportChronicle 631-644。
- 断绝卡挂点（加「生成定论卡」按钮）：`live.js:1036-1054` showExtinctCard。
- 分享图骨架（1280×800 改 1080×1440）：`meta.js` shareImage 一线，roundRect/wrapText 复用。
- 导出带 summary：saveToExportDoc；导入渲染；挑战码入口：封面绑定；newWorld 透传。
- 纪元/命名素材：`live.js:11-24` ERA_NAMES/ERA_FLAVOR；`namer.py:32-44` 时代名池。

### 2.5 分期路线

- **P1（最小切片）**：检测器 v1（3 规则）+ showExtinctCard 加定论卡（复用海报绘制）→ 断绝即得可转发一图。
- **P2**：章回族史 tab + txt 导出 + 模板库 v1（约 130 条）。
- **P3**：挑战码编码/解码 + 输码开局 + 对照卡 + 存档 summary。
- **P4（可选）**：MediaRecorder 回放 webm、批量局合订本。

### 2.6 风险与开放问题

① 文案分档若不足，「讣告同质化」会杀死转发欲——需真实跑多 seed 验证弧线命中率（尤其险断复兴出现率是否 ≥20%）；② 挑战码跨版本漂移（sanitizeOverrides 净化已知键，码内须带 version）；③ canvas 楷体跨平台回退；④ 章回与村志的信息重复——村志保持流水，族史只做提炼，二者定位需在 UI 上明确区分。开放问题：定论卡是断绝时自动弹出还是手动入口（建议自动弹+可关）。

---

## 第 3 章 · 方向 C「承祧与禀性」（深度 · 模拟层 · 中期）

### 3.1 愿景

让血脉存亡从裸骰子变成人事：断绝率这条数学曲线仍是显式参数，但玩家看到的是过继、招赘、禀性这些真实宗族机制里「人」的挣扎。

### 3.2 三轮迭代留痕

**第一轮 · 发散（10 项）**。调研锚点：CK3 先天特质双亲同点升阶、性格 16 岁定型但被社区讥为「静态贴纸」；DF 特质 = token+数值直接驱动行为；史料：立嗣须「昭穆相当、由近及远（同父周亲→缌麻）、凭族长」，招赘为外姓承祧变通，分家析产生成宗族；Galton-Watson：μ>1 时灭绝率仍约 1/μ，百年内 80-90% 男系断绝是数学必然，且实证「高地位支系断绝率更低」——承祧机制有史料与数学双重合法性。
①过继立嗣 ②招赘婚 ③三槽特质 ④分家支系着色 ⑤族田/宗祠/族学轻经济 ⑥功名声望 ⑦灾异多样性 ⑧字辈谱 ⑨genes 显性遗传 ⑩母系传承开关。

**第二轮 · 批判**（深度/扰动可控/成本/性能/复现契合）：

| 候选 | 一句判决 |
|---|---|
| ① 过继 | 深度+++，把 E2/E3/E6 转为续线，事件驱动零扫描成本——**核心保留** |
| ② 招赘 | 与过继成对（血存姓不存 vs 姓存血不存），复用撮合——**并入承祧包** |
| ③ 三槽特质 | 全局乘数扰曲线——**砍成两槽、均值中性**；技艺槽无经济承载，淘汰 |
| ④ 支系着色 | 独立机制扰动大——**降为展示层派生**（从嗣父链/五服推） |
| ⑤ 族田经济 | 全局耦合 K/婚愿、需经济循环、难校准——**淘汰**，远景以功名代理 |
| ⑥ 功名声望 | 有实证但缺承载——**并入过继作 V2 二阶规则** |
| ⑦ 灾异 | 死亡潮已有，纯文案分档零扰动——**并入 flavor 层** |
| ⑧ 字辈 | 纯 name 子流零扰动、辈分感强——**保留最小切片** |
| ⑨ genes 落地 | 与特质重复——**淘汰**，表型 = ln_vulnerability 离散化即可 |
| ⑩ 母系开关 | 一行核心改动+参数开关，性价比极高——**搭车保留** |

### 3.3 核心设计详案

**C1 承祧包（过继+招赘）**
- 玩家感知：村志「张甲无嗣，立同宗侄张丙为嗣」「招李丁入赘，承张家香火」。
- 规则：step_year 在断绝检查前插入 `_inheritance`——对无子血脉男（亡故/过育龄），按同父周亲→缌麻顺序找昭穆相当且有兄弟的未婚侄辈，以 `adoption_prob` 立嗣：嗣子 lineage=True、改姓嗣父、记 `heir_of`。招赘：血脉男有女无子时外姓婿入赘，子女姓随母系祖父（血存姓不存）。
- 数据落点：Person 加 `heir_of: int|None`、`adopted: bool`——**不改 father_id/mother_id**，保住 kinship 亲等缓存「父母出生即定」的硬前提。
- 参数：`adoption_enabled=False / adoption_prob / adoption_max_depth=3（五服）/ uxorilocal_enabled=False`，入行为开关区，默认关 = 旧 seed 逐字节复现。
- 数值节奏：无嗣夫妻约 20-25%，五服内有可嗣侄辈者过半，adoption_prob=0.7 时断绝率约 90%→78%（batch 实测校准）；事件数年一遇，天然稀疏。
- 断绝曲线影响：显著下移且由单参数线性可控——「90% 断绝」从物理常数变成玩家可选的文明参数；E4（外迁）不受救，嗣子被抽走反可致过继链再断，保留张力。

**C2 禀性两槽（体质/性格）**
- 玩家感知：面板一行「体弱多病，性刚烈」；讣告婚配文案差异化。
- 落点：Person 加 `traits: tuple|None`；体质由 ln_vulnerability 离散化（零新随机），性格走新增 `"trait"` 子流（按名 SHA-256 派生，不动既有七流）；仅约 30% 人口给显性 tag 防信息过载。
- 影响：体弱死亡 ×1.2、康健 ×0.85（均值中性校准）；性格对撮合候选次序固定微扰。断绝曲线近零漂移，batch 前后对照验收。

**C3 字辈谱 + 灾异（叙事层）**
- 字辈：开局 flavor 流定型 20 字世代诗，血脉男丁名 = 姓 + 字辈[gen] + 名（开关默认关）；灾异：harvest 池按 hardship 细分旱/蝗/疫/兵。零模拟扰动。

**对断绝路径的影响方向**：E2（未婚未育）/E3（早夭）/E6（已婚未育）→ 承祧可救，转续线；E1（始终无男丁）→ 部分可救（需五服内有侄）；E4（外迁）→ 不救，保留宿命。

### 3.4 代码落点

- `person.py:8-28`：加 heir_of/adopted/traits 字段
- `village.py:184-206`：流水线插 `_inheritance`（在 village.py:206 断绝检查前）
- `village.py:532-577`：`_classify_extinction` 加「已立嗣」豁免（E2/E6）
- `village.py:154`：lineage 判定改「生父或嗣父链」；`village.py:148-157`：spawn 姓继承接入招赘改姓与复姓兼容
- `village.py:266-316`：撮合接入入赘分支；`village.py:473-518`：灾异分档
- `params.py:74-79`：行为开关区加 adoption_*/uxorilocal/generation_poem/traits_enabled
- `rng.py:12`：STREAMS+"trait"；`namer.py:60-63`：字辈挂点；`engine.py:65-69`：开局世代诗
- `kinship.py:10,18-36`：亲等缓存安全性是过继设计硬约束（故不动父母字段）
- `tests/test_core.py`：加默认关逐字节复现、开后断绝率扫描、复姓过继、断绝豁免四类测试

### 3.5 分期路线

P1 过继最小切片（规则+村志文案+断绝豁免+batch 校准）——单独可玩：「看旁系的儿子救回一脉」；P2 招赘+字辈开关；P3 禀性两槽+面板呈现；P4 支系着色（派生）、灾异细化、母系开关；V2 功名/族田代理变量。

### 3.6 风险与开放问题

1. 改姓与 surname_len/复姓交互（village.py:148-157）需细测；
2. `_classify_extinction` 的 E1 兜底在立嗣案例上可能误报，分类器要重想；
3. adoption_prob 默认值是设计立场——是否提供「史实档/裸数学档」双预设；
4. 隔代立嗣的尊卑倒置是否禁止；
5. 招赘使姓氏排行统计失真，UI 需标注「姓存血不存」；
6. 找侄辈的亲等查询须复用 `_anc_cache`，5 万人下 `_inheritance` 为 O(血脉男数)，可忽略。

---

## 第 4 章 · 方向 D「族史长廊」（留存 · 元层 · 速赢期）

### 4.1 愿景

把每一次观察变成可收藏、可对比、可约战的一段族史：**图鉴**负责「断绝也值得纪念」，**考卷**负责「打开就知道看什么」，**名人堂与周挑战**负责「下周还有一局等你」。

### 4.2 三轮迭代留痕

**第一轮 · 发散（8 个）**。调研锚点：roguelite 元进度三原则——解锁当渐进教学、失败也给奖、勿硬门槛；Wordle 留存三角 = 每日一局的稀缺 + 全员同题（日期→确定性 seed）+ 无剧透对比码，纯 localStorage 可成；图鉴留存关键 = 可见进度+事件性获取优于刷，否则得「图鉴问题」。
①结局图鉴 ②挑战考卷 ③名人堂 ④周挑战 ⑤交互式引导关 ⑥机制渐进解锁 ⑦封面回访钩子 ⑧成就分层框架。

**第二轮 · 批判**（动机真实/成本/无服务器兼容/纯净气质冲突）：

| 候选 | 判决 |
|---|---|
| ① 结局图鉴 | 动机强（断绝率高，每种死法皆常见可遇）· 成本极低（outcome.reasons 现成）· 零冲突——**核心** |
| ② 挑战考卷 | 动机真实（正中「不知看什么」短板）· 成本中（判定复用 ctxFromSnap）· 走 overrides 白名单不破确定性——**留** |
| ③ 名人堂 | 观察型玩家天然爱「看故事」· 成本低（collectSaveData 已算 max_kids/oldest）——**留** |
| ④ 周挑战 | Wordle 已验证 · 成本低（日期派生 seed）· 本地时钟可改属同款取舍——**轻量留**（只做对比码不做排名） |
| ⑤ 引导关 | 动机真实但成本中高，强制脚本与「扔进世界自己悟」气质冲突度高——**降级**为一次性可关轻提示 |
| ⑥ 机制渐进解锁 | 本作无「变强」叙事，解锁参数=暗示旧参数不完整的作弊器，破坏实验纯净性——**淘汰** |
| ⑦ 封面钩子 | 单独成系统动机弱——**并入** ④③ 的封面一行 |
| ⑧ 成就分层 | 纯基础设施 · 零冲突——**留**，作 ①—④ 的挂载框架 |

**第三轮 · 取舍**：以 ① 图鉴为 soul（被动收录、零打卡）、② 考卷为目标感、③④ 为回访钩子、⑤⑧ 为辅助框架；淘汰 ⑥、不独立做 ⑦。自批：图鉴/名人堂均为终局被动收录，无时限无每日，是「回忆册」非「任务列表」，不构成打卡负担——但未来若加「集齐奖励」须防滑向任务化；引导仅一次性可关，保住「自己悟」。

### 4.3 核心设计详案

**1. 断绝图鉴「万殁谱」**｜感知：成就殿堂旁新增册子，8 格集邮，未收格显示剪影。｜规则：终局自动存档时（dueFinal）读 outcome.reasons 逐码收录，存续结局按 extinct_year/finished 判定；每格记首例档案（seed/年份/血姓）。｜键：`fjs1_codex {v, seen:{E1_no_male_born:{seed,year,bloodName,ts}}}`。｜注意：sim 实际产出码仅 E1-E4 + E6（E5/LIVELOCK 只存在于 DESIGN.md 与常量定义，代码从不赋值），图鉴格须与实际对齐。示例条目：始终没有男丁出生 / 男丁早年夭折 / 男丁外迁离村 / 已婚终生未育 / 百年不坠 / 千年不息。

**2. 挑战考卷「族史五卷」**｜感知：封面「考卷」按钮→关卡表，卡面显示已得评级（甲/乙/丙）与引语。｜规则：每关 = {固定 seed，白名单内 overrides，胜利条件（复用 ctxFromSnap 字段），评级阈值}；开局经 Meta.worldStarted 带参，判定挂 Meta.gameTick 每 tick（胜利即横幅）+ 终局评级；键 `fjs1_challenges {v, done:{L1:{grade,year,ts}}}`；关卡表为 meta.js 新增 CHALLENGES 常量，纯数据零依赖。｜示例五关：
- 乱世孤苗（hardship_wave_prob=0.25，活到 y100 甲 / y50 乙）
- 不孕之咒（parity_cap_base=2，见第 4 代）
- 千男之村（村 800 + 封闭内婚，活到 y200）
- 外迁之殇（protect=0，y150 前男丁不断）
- 千年不息（全默认活满）

**3. 名人堂「族中先贤」**｜感知：封面一行「史上最寿：李氏某公 94 岁（seed 42）」。｜规则：终局以 collectSaveData 的 stats 与纪录比较，破纪录收录并保留前任；键 `fjs1_hof {v, records:{oldest, mostKids, maxGen}}`。

**4. 周挑战「本周同源」**｜感知：封面副行「第 36 周 · 本周同源：生育×0.8」。｜规则：week = floor((now−epoch)/7d)，seed = 20260827 + week×7919，参数取常量表；完成生成对比码 `fjsW36:412y/9代/断绝E3/男丁峰值7`，纯文本可贴群；键 `fjs1_weekly {week, played, bestCode}`。

**5. 轻引导「初见批注」**｜感知：首局顶部一次性提示条（「金线归零即血脉断绝 / 点人物看他一生 / +10 年快进」），点 ✓ 永不再现；键 `fjs1_settings.onboarded`；文案用族谱批注腔不用教学腔。

**6. 成就分层+隐藏接口**｜ACHV 条目扩 `{tier:'铜|银|金', hidden}`；隐藏未解锁显示「？？？」；evalAchievements 已收 events 参数，可挂事件型隐藏成就（如「lineage_danger 后存活满 20 年的翻盘者」）；分享图网格自适应扩展。

### 4.4 代码落点（meta.js 为主线）

- ACHV 定义区加 tier/hidden 字段；AchvStore 模式复制出 Codex/Hof/Challenge 三 Store；DEFAULT_SETTINGS 加 onboarded。
- KNOWN_OVERRIDES 已覆盖全部参数键，挑战 overrides 直接过 sanitizeOverrides。
- gameTick 内加 evalChallenges(ctx)；worldStarted 接受 challengeId 存入 _run；TEST 表即挑战判定样板；maybeAutosave 的 dueFinal 分支 = 图鉴/名人堂/周挑战终局收录挂点。
- refreshCoverInfo 加周挑战与名人堂一行；shareImage 泛化为 drawPoster(cards) 复用于图鉴/周码海报；bindDialogButtons 加考卷/周挑战按钮（封面菜单在 live.html 加两枚）。
- renderAchvGrid 加 hidden 分支；renderOverview 旁新增 renderCodex/renderChallenges。
- live.js：showExtinctCard 加「已收录图鉴 n/8」与挑战未达标提示；collectSaveData 已含 max_kids/oldest_age/lineage_male——名人堂数据源零改动；Meta.gameTick 已透传 events；dlg-new 表单不动，挑战开局不经表单。
- 约束核对：全部落 `fjs1_*` localStorage，不新增服务器调用，零新依赖，旧 seed 复现不受影响（挑战参数只经 overrides）。

### 4.5 分期路线

- **P1（约半天）**：断绝图鉴 8 格 + 断绝卡收录提示 + ACHV hidden 字段接口。切片 = 断一次即收藏一段历史。
- **P2**：考卷 5 关 + 评级 + 面板。切片 = 点关→带参开局→终局评级横幅。
- **P3**：名人堂 + 周挑战 + 封面钩子 + 轻引导。切片 = 封面出现「本周同源」与先贤一行。
- **P4**：海报泛化（图鉴版/周码版）+ 首批事件型隐藏成就。

### 4.6 风险与开放问题

挑战胜利条件须用 alive 口径，防「断绝后继续观察」误判；周挑战本地时钟可改、对比码无签名可伪造——荣誉用途可接受（Wordle 同款取舍）；ban_kin_side=∞ 封闭内婚是否被 sim 支持需实测；图鉴是否为 E5 留空格或定案 8 格；观察局中途暂停退出是否算挑战失败（建议允许续打，读档后凭 _run.challengeId 续判）。

---

## 第 5 章 · 协同与总路线图

### 5.1 三层推进

```
速赢层（只动 viewer/meta.js，1-2 天级）
  ├─ B-P1 弧线检测器 v1 + 断绝定论卡（断绝即得可转发一图）
  ├─ D-P1 万殁谱图鉴 + 成就 hidden 接口
  └─ D-P2 挑战考卷五关（走 overrides 白名单，零架构改动）
中期层（动 sim_core，需批跑校准）
  ├─ C-P1 过继立嗣（规则+文案+断绝豁免+batch 校准）
  ├─ C-P2 招赘 + 字辈开关
  └─ C-P3 禀性两槽
愿景层（动存档格式 v2 与重放协议）
  ├─ A-P1 族老裁决可选模式（危急卡+香火+decisions 入档）
  ├─ B-P2/P3 章回族史 + 挑战码闭环
  └─ D-P3/P4 名人堂 + 周挑战 + 海报泛化
```

### 5.2 依赖与复用

- **B 的挑战码 = D 周挑战的分享载体**：先做 B-P3 的编码器，D-P3 的周挑战直接产出可贴群对比码。
- **shareImage 泛化 drawPoster 后**，成就/图鉴/定论卡/周码四类海报共用一套绘制底座。
- **C 是 A 的机制土壤**：过继/招赘先作为自动机制存在（C），未来可被 A 的裁决卡包装成玩家抉择（「是否立嗣」卡）——决策层与模拟层各自独立成立，又能互相加深。
- **A 的存档 v2 必须在 C 落地之后设计**（合并 heir_of 等新字段一次到位，避免存档格式两次翻版）。

### 5.3 每期验收标准（回归红线）

1. **复现红线**：一切新参数默认关闭时，旧 seed 输出与现版本逐字节一致（tests 17 项全过 + 新增默认关逐字节测试）。
2. **校准红线**：C 每个机制落地后跑 `runner.batch --n 200` 对照断绝率/原因分布，偏差写入 DESIGN 文档。
3. **确定性红线**：A 的决策序列重放必须逐字节还原；decree 流不得污染其他子流。
4. **性能红线**：5 万人规模年度步进毫秒级不回退；前端每帧重排不劣化。
5. **体验红线**：纯观察模式永远完整可玩；「听天由命」永远在场。

---

## 附录 · 勘察事实清单（设计依据，探索时核实值）

**A1. 年度流水线**（village.py:184-206）：纪元宣告 → `_births` → `_matchmaking` → `_deaths` → `_migration` → `_lineage_danger` → `_vital_event` → `_flavor_events`（flavor_events=True 时）→ `_record` → `_check_extinction`。

**A2. world_events 全 17 种 kind**（village.py 行号）：`era{year,band}`(193)；`lineage_birth{year,id,name,father,mother}`(231)；`lineage_infant_death{year,id}`(239)；`lineage_marriage{year,ids[2]}`(305)；`lineage_death{year,id,age,children}`(331)；`lineage_migrated{year,id}`(370)；`weddings{year,count,ids}`(313，仅 flavor 开)；`emigration_wave/emigration{year,count,names[:3]}`(392-400)；`immigration{year,count,names[:3],from?}`(424)；`lineage_danger{year,count}`(447)；`vital{year,born,died,emigrated,immigrated,span}`(465，每 5 年)；`harvest{year,text}`(484，6% 概率)；`flavor{year,text}`(490，4%)；`elder_death{year,id,age}`(498，≥75 岁最长逝者)；`coming_of_age{year,id}`(503，血脉男丁 16 岁)；`milestone{year,pop,tier}`(514，档位 200/300/500/800/1300/2100/3400)；`extinct{year,reasons}`(540)。

**A3. history 行字段**（village.py:521-530）：year, pop, lineage_male, lineage_male_adult(≥16), born, died, emigrated, immigrated, wave(bool)。

**A4. hardship 作用点**：常态外迁率 `min(0.3, 0.002×(1+hardship×6))`(373)；迁出潮概率 `0.05×(0.3+hardship×4)`(378)、规模 `min(0.5, 0.08×(0.5+hardship))`(380)；丰歉/杂闻文案分档（≥0.30 灾 / ≤0.10 丰）(428,482-489)。

**A5. era 机制**：带 = year//10；首带从均值抽，后续 7 参数 AR(1)（ρ=0.6，birth 走 ln 空间）(84-110)；钳制：K≥50、sex_ratio[80,130]、infant_mort[0.001,0.5]、mort_scale≥0.2、willingness[0.3,0.99]、hardship[0,1](112-121)。

**A6. 断绝检查**（village.py:532-577）：在世男血裔=0 且 pending_male_fetuses==0 → extinct。分类：E4=有男血裔且全 migrated_out；E2=死亡男血裔未婚且卒年≥30；E6=死亡已婚无子女且卒年≥min_marry_age；E3=全部死亡者<16 岁；兜底 E1。**E5/LIVELOCK 仅存在于 DESIGN.md 与 T_LIVELOCK 常量(35)，代码从不赋值**。

**A7. Params 要点**（params.py:22-92）：end_year 1000、pop_cap_total 50_000、founder_village_size 300、min_marry_age 16、couple_age_gap 10、ban_kin_side 2、remarriage False、parity_cap_base 5、band_years 10、era_rho 0.6、infant_mort 0.15→0.005（halfway 600）、anchor_mode "transition"、hardship_wave_prob 0.05、matchmaking_per_year None、lineage_emigration_protect 1.0、flavor_events True。EraParams 7 字段。scale_for = dataclasses.replace，**接受任意 Params 字段名作 overrides**。mortality_anchor：古/今两张 16 档年龄桶死亡率表线性插值。

**A8. Person 字段**（person.py:9-28）：id, father_id, mother_id, birth_year, sex, death_year, spouse_id, ln_fertility, ln_vulnerability, fertility_cap(None，未用), children_born, migrated_out, founder, lineage, name, surname_len, genes(M3 预留空 dict)。

**A9. kinship 公开 API**：ancestor_chain(18-36)、is_lineal(39-44)、collateral_distance(55-64)、marriage_banned(67-75)。ban_kin_side：亲等=共同祖先处分叉深度取 max；直系恒禁；旁系 d≤k 禁；无共同祖先（移民/创始者）放行。缓存按 id 定型安全（父母出生即定）。

**A10. seed_village**（engine.py:29-72）：birth 子流生成 300 人创始村（男 150→女 150，年龄 integers(0,60)），ln_* ~ N(0,0.2)，name 子流起名；焦点夫妻男 20 女 18，夫 lineage=True，可自定义姓名。终止判序：断绝→year≥end_year→pop_cap。

**A11. RNG**：7 子流 ("era","match","birth","death","migrate","name","flavor")；每条 default_rng(SHA-256(f"{master}:{stream}") 前 8 字节)。

**A12. 前端协议**：/api/step 增量合并（live.js:1076-1111）——year、history_tail 按年去重、era_new 按 band 去重、delta_people upsert、world_events、extinct（弹断绝卡）、stats、mortality_curve；stepYears 每批 ≤50 年、排队 ≤200；断绝后仍可 step（server.py:119-121）。world_meta={seed,overrides,founder} 随快照下发（server.py:177）。

**A13. tests/test_core.py 17 项**：亲等 7、复现 2（同 seed 逐字段+history 相等）、性别比频率 1、终止态 1、行为开关 3（撮合上限/外迁保护/E6 归类）、R1 3（复姓继承/flavor 隔离 digest/80 年内必出风物）。

**A14. 性能**：pop_cap_total=50_000（累计人口算力上限）；前端「每帧全量重排：5 万人 21ms（node 实测）」（live.js:234）；批跑 results/m2_full：多数 run 数十年内 EXTINCT_BLOOD（E1 为主），POP_CAP 个体约 280-298 年触顶。

---

*探索方法备忘：四方向各由独立探索线完成——第一轮候选发散（含业界调研）→ 第二轮五维批判打分淘汰 → 第三轮幸存方向深挖+最坏情况自批；另有一轮全代码只读勘察核实全部落点。被否方案与判决理由保留在各章迭代留痕中，供未来重启时跳过已证伪的路径。*
