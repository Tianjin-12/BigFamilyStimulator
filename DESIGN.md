# 家族模拟器 设计文档 v1

> 目标：从一对创始夫妻出发，在有限村庄中模拟数百年人口演替，观察父系血脉能否永续、在何种参数下断绝。
> 日期：2026-08-23

## 0. 三层架构

```
sim-core/   纯模拟核心：纯函数式、种子注入、无 IO、无随机性逃逸
runner/     批量运行 + 聚合统计（CSV/终端报表）
viewer/     网页前端：滚动谱系树 + 人口曲线 + 参数图表 + 个人侧栏
```

- `sim-core` 不 import runner/viewer 的任何东西；runner 只通过公开 API 调用 core。
- 所有随机性来自注入的 RNG，同 seed 必须逐字节复现（含浮点运算顺序稳定）。

## 1. 世界观

### 1.1 有限村庄（全员模拟，无外部凭空生成）

- 村庄 = 模拟宇宙中全部人口。**所有人都是同构 Person**，走同一套生老病死婚育规则。
- 焦点家族只是**透镜**：标记创始夫妻的父系血脉，其余家族自然涌现，零特殊逻辑。
- 不做"外部配偶池"：M3 需要配偶真实基因型与真实祖先（含外嫁女儿——她仍在村里，是别人家的妻子），凭空生成会切断这两条路。

### 1.2 村庄承载上限 K（时期参数）

- 人口 > K：随机青年外迁（移出模拟）。外迁是合法的血脉流失原因，焦点家族男丁外迁**不豁免**。
- 人口 < K/4：少量移民补充（移民无本地祖先，亲等判定中视为与所有人无关）。

## 2. 家族与终结（口径）

- **家族 = 父系血脉**（ Y 染色体传递口径，随夫姓）。
- **主判定：血脉断绝** = 无在世男血裔 且 无待产男血裔胎儿（含因外迁流失殆尽的情形）。判定不可逆，触发即记录断绝时刻，其后模拟可终止或继续跑全村。
- 次级记录：物理无活人、焦点家族全体后代清零。

### 2.1 断绝原因分类（核心输出）

| 代码 | 原因 | 判定 |
|---|---|---|
| E1 | 无男丁出生 | 断绝时最后一代无男性，未发生婚配失败 |
| E2 | 男丁未育 | 有男性但终身未成婚/成婚未育 |
| E3 | 男丁早夭 | 男性因夭折/死亡于育龄前清零 |
| E4 | 男丁外迁 | 在世男血裔全部外迁 |
| E5 | 活锁 | 村内婚配池被亲等规则耗尽，有育龄者永远撮不成对 |

（一次断绝可多因并存，全部记录。）

### 2.2 性别比

- 出生性别比为**时期参数**，默认 105:100（男:女），可调——父系存活对男丁比例敏感，这是正式实验旋钮而非噪声。

## 3. 时间模型

- **按年推进**。每人有出生年，年龄逐年 +1。
- **时期： 每 10 年一个时间带**，时代参数按带取值。
- 时代参数抽样：每带随机抽取，相邻带 AR(1) 自相关 ρ=0.6，防锯齿跳变。
- "代"是事后统计量（如按首胎年龄切），不参与驱动。

## 4. 禁婚规则（可调参数）

- 亲等判定：沿家谱上溯找共同祖先；直系（上下行链）与旁系（平行链）分开算。
- 默认：**直系血亲无限代禁止；旁系三代以内禁止**（法律标准）。
- 参数化为禁婚旁系亲等 k（默认 2），k=∞ 即封闭家族内婚——留给实验。

## 5. 个体模型

### 5.1 Person 字段（v1）

```python
Person:
  id: int
  father_id, mother_id: Optional[int]   # 移民/创始者为 None
  birth_year: int
  death_year: Optional[int]
  sex: 'M' | 'F'
  spouse_id: Optional[int]              # 一夫一妻
  genes: {locus: [allele, allele]}      # M3 预留；M1 单一虚拟位点 fertility
  # 派生：age(t), is_alive(t), 男血裔判定沿 father_id 链
```

### 5.2 可遗传个人属性

- fertility（生育力）、vulnerability（脆弱度/死亡倾向）：出生时从父母定型，终身不变。
- 遗传公式：`ln(child) = (ln(father) + ln(mother))/2 + ε`，ε ~ N(0, σ)，σ=0.3。
- cap=0 机制：终身子女上限 round 后下限为 0 → 天然出现不孕夫妻。

## 6. 每年事件流水（顺序固定，保证可复现）

```
对每一年 t（当前时期带 B = band(t)）：
  1. 出生判定   对每个在婚且未达终身上限、女方在生育窗口内的夫妻：
                  概率 p = 年基准(B) × fertility(妻) × fertility(夫) → 生育
                  新生儿：性别 ~ Bernoulli(sex_ratio(B))，遗传属性定型，
                  婴儿当年按夭折率判定
  2. 撮合       对每个达到婚龄、未婚、在村的适婚者（随机顺序）：
                  在未婚池中找满足【非禁婚亲等 + 年龄差约束】的对象，
                  双方各有成婚意愿概率（时期参数），撮合即结婚
  3. 死亡判定   对每个在世者：p = age_sex_mortality(B) × vulnerability → 死亡
  4. 迁移       人口 > K → 随机青年外迁；人口 < K/4 → 补充移民
  5. 断绝检查   主判定触发 → 记录时刻与原因分类，标记结果
```

- 孕期占位：生育成功当年即占用（女方当年不再参与生育判定），等效一年一胎上界。
- 丧偶不再婚（flag：`remarriage`，默认 False）。

## 6bis. 参数组合形式（统一约定）

所有速率类参数：`实际值 = 时期基准(B) × 个人乘数 × Bernoulli(事件扰动)`

- 时期基准：出生性别比、生育年概率、年龄×性别死亡率、婴儿夭折率、K、婚龄、年龄差窗口、成婚意愿。
- 个人乘数：fertility（生育）、vulnerability（死亡），出生定型。
- 分层原则：个体间差异 ≠ 年度噪声；个体属性不随年重抽。

## 7. 终止态（枚举）

| 终止态 | 含义 |
|---|---|
| EXTINCT_BLOOD | 血脉断绝（主判定） |
| TIME_LIMIT | 到达时间上限（默认 1000 年） |
| POP_CAP | 累计人口超算力上限 |
| LIVELOCK | 全村适婚者两两禁婚，且无移民/新生儿可解 |

## 7bis. RNG 分流

- 子流：era（时代参数）、match（撮合）、birth（生育/性别）、death（死亡）、migrate。
- 各子流独立 seed（由主 seed 派生），任何一处参数微调不破坏其他子流的复现性。

## 8. 输出

- 每次运行：谱系图数据（全 Person + 婚姻边）+ 逐年人口序列 + 事件日志 + 断绝记录（时刻/原因）。
- 批量运行聚合：断绝概率、存续代数分布、断绝原因分布、平均代际间隔、总人口曲线（中位数/分位带）。
- 验证清单：
  - [ ] 同 seed 复现性测试（逐字段相等）
  - [ ] 大样本频率 ≈ 参数（性别比、生育率、各年龄段死亡率）
  - [ ] 亲等判定单元测试：兄妹/父女/堂表/第二表/第 4 代直系/移民 vs 全员

## 9. 里程碑

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 人口核 | Person/村庄/每年流水/血脉判定/断绝分类 | CLI 跑单次：断绝时刻+原因+人口曲线 |
| M2 统计与可玩性 | runner 批跑 + 聚合报表 + viewer v1（谱系树/人口曲线/点击侧栏） | 1000 次批跑出断绝概率分布 |
| M3 基因 | genes 多位点：杂合/纯合、显/隐性、性连锁 | 遗传病在谱系中的分布可视化 |

## 10. 默认参数表（v1，全部可调）

| 参数 | 默认 | 说明 |
|---|---|---|
| 初始人口 | 创始夫妻 + 村庄初始 ~300 人 | 无本地祖先 |
| 婚龄 | ≥16（双方） | |
| 夫妻年龄差 | ≤10 岁 | 双向 |
| 女方生育窗口 | 16–45 | |
| 生育年概率基准 | 0.25/年 | 时期可调 |
| 终身子女上限基准 | 5 | × 夫妻生育力 × 噪声，下限 0 |
| 出生性别比 | 105:100 | 时期参数 |
| 婴儿夭折率 | 0.15（古代带）→ 0.005（现代带） | |
| 成人死亡率 | 年龄×性别曲线，时期插值（古代/现代两锚点） | |
| K | 600 | |
| 移民补下限 | K/4 = 150 | |
| 禁婚旁系亲等 k | 2 | k=∞ → 封闭内婚实验 |
| σ（遗传噪声） | 0.3 | |
| ρ（时代带自相关） | 0.6 | |
| 批跑次数 | 1000 | M2 验收 |
| 时间上限 | 1000 年 | |
```
